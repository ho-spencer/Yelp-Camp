<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=, initial-scale=1.0">
        <title>Edit <%= campground.title %>
        </title>
    </head>

    <body>
        <h1>Edit<%= campground.title %></h1>
        <form action="/campgrounds/<%= campground._id  %>?_method=PUT" method="POST">
            <div>
                <label for="name">Update Name</label>
                <input type="text" name="campground[title]" id="name" value="<%= campground.title %>">
            </div>
            <div>
                <label for="location">Update Location</label>
                <input type="text" name="campground[location]" id="location" value="<%= campground.location %>">
            </div>

            <button>Update Campground</button>
        </form>

        <a href="/campgrounds/<%= campground._id  %>">Back to Campground Details</a>

        <p>
            <a href="/campgrounds">All Campgrounds</a>
        </p>

    </body>

</html>